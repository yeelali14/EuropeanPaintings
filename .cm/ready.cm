# -*- mode: yaml -*-


manifest:
  version: 1.0

automations:
  jira_ticket:
    if:
      - {{ pr.description | match(regex=r/^- \[ \]/) | some }}
    run:
      - action: add-label@v1
        args:
          label: "incomplete-step"
          color: 'F6443B'