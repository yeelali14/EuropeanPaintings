# -*- mode: yaml -*-

manifest:
  version: 1.0

automations:
  testing_only:
    if:
      - {{ pr.labels | match(term='testing') }}
    run:
      - action: add-comment@v1
        args:
          comment: |
            {{ console.log(process.env) }}

