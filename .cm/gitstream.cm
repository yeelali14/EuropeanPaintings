manifest:
  version: 1.0

automations:
  pr_title:
    if: 
      - {{ pr.labels | match(term='bug') | some }}
    run:
      - action: add-comment@v1
        args:
          comment: |
            title changed !
            
